# ynabUp
An Up Bank → YNAB transaction sync service, written in Go.

![Architecture diagram](ynabUpDesign.svg)

Diagram source is `ynabUpDesign.d2`. GitHub doesn't render D2 natively, so
after editing it, regenerate the SVG with `d2 ynabUpDesign.d2 ynabUpDesign.svg`
and commit both.
